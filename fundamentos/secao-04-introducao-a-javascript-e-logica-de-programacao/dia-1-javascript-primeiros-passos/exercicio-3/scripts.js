const valorUm = 10;
const valorDois = 20;
const valorTres = 30;

if (valorUm > valorDois && valorUm > valorTres) {
  console.log(valorUm + " é maior que " + valorDois + " e " + valorTres);
}

if (valorDois > valorUm && valorDois > valorTres) {
  console.log(valorDois + " é maior que " + valorUm + " e " + valorTres);
}


if (valorTres > valorDois && valorUm < valorTres) {
  console.log(valorTres + " é maior que " + valorDois + " e " + valorUm);
}
